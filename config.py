# config.py

# SerpAPI_API_KEY = "YOUR_SERPAPI_API_KEY"  # IMPORTANT: No longer used directly by the script for fetching.

# User-defined search parameters (will be filled by user input in main.py)
# SEARCH_KEYWORD = "" # No longer used for fetching
MIN_PUBLICATION_YEAR = 0 # Still used for filtering JSON data
MIN_IMPACT_FACTOR = 0.0 # Still used for filtering JSON data
# PREFERRED_ARTICLE_COUNT = 0 # No longer used for fetching

# New parameter for JSON input
JSON_FILE_PATH = "input_articles.json" # Default path, can be overridden by user input
